<view wx:if="{{status==1}}">
    <ShowUserInfo
      bindeditUserInfo="editUserInfo"
      nickName="{{userInfo.nickName}}"
    />
</view>
<view wx:elif="{{status==2}}">
    <EditUserInfo
      infoLists="{{infoLists}}"
      nickName="{{userInfo.nickName}}"
    />
</view>

